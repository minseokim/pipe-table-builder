import React from 'react';
import { Pane, majorScale } from 'evergreen-ui';

const Header = () => {
  return <Pane background="tint1" height={majorScale(7)} border="default" />;
};

export default Header;
